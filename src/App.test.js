import { render } from '@testing-library/react';
import App from './App';

// import * as React from 'react';


test('App test', () => {
  const linkElement = 1 + 1;
  expect(linkElement).toBe(2);
});
